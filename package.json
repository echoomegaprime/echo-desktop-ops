{
  "name": "@echoomegaprime/echo-desktop-ops",
  "version": "1.0.0",
  "description": "Governed desktop ops MCP \u2014 focus, notify, screenshot meta for ECHO fleet consoles. No raw shell, no pixel dumps to models.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "echo-desktop-ops": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "start": "node dist/cli.js",
    "dev": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/echoomegaprime/echo-desktop-ops.git"
  },
  "keywords": [
    "echo",
    "mcp",
    "oauth",
    "echoomegaprime",
    "echo-desktop-ops"
  ],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
